# Backend for my portfolio website

## Configuration

This application uses environment variables for sensitive configuration. See [CONFIGURATION.md](CONFIGURATION.md) for detailed setup instructions.

### Quick Start

1. Set required environment variables (DB_URL, DB_USERNAME, DB_PASSWORD)
2. Run the application: `./mvnw spring-boot:run`

For detailed configuration instructions, see [CONFIGURATION.md](CONFIGURATION.md).
